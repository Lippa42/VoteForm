{
  "name": "sfide-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Client web (Visualizzatore + Spettatore) serviti dall'host Android. Mostrano lo stato e inviano intenti via WebSocket.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
